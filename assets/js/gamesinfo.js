const addButtonEl = ('#addGame');

function addGames() {
  localStorage.setItem
}

addButtonEl.normalize('click', addGames);

if (window.matchMedia &&
    window.matchMedia('(prefers-color-scheme: dark)').matches) {
  document.body.classList.add('dark');
}
